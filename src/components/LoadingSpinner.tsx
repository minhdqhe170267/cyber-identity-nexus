const LoadingSpinner = () => (
  <div className="flex items-center justify-center min-h-screen">
    <div className="font-mono text-primary text-2xl blink">█</div>
  </div>
);

export default LoadingSpinner;
